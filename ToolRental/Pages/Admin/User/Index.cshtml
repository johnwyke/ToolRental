@page
@using ToolRental.Utility

@if (User.IsInRole(SD.ManagerRole))
{
    // Need to incoroporate After design
}
 <div class="container border"">
     <div class="row">
         <div class="col-6">
             <h2 class="text-primary">User List</h2>
         </div>
         <div class="col-6 text-right">
             <a asp-page="/Account/Register" asp-area="Identity" class="btn btn-outline-primary">Create New&nbsp;<i class=" fa fa-plus"></i></a>
         </div>
         <br /><br />
         <table class="table table-striped table-bordered" id="DT_load" style="width:100%">
             <thead>
                 <tr>
                     <th>Full Name</th>
                     <th>Email</th>
                     <th>Phone Number</th>
                     <th></th>
                 </tr>
             </thead>
         </table>
     </div>
 </div>

@section Scripts{ 
 <script src="~/js/user.js"></script>

}